{
  "name": "aanya-portfolio-backend",
  "version": "1.0.0",
  "description": "Full-stack Node.js & Express backend for Aanya Chaudhary's portfolio.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node prepare-build.js && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
